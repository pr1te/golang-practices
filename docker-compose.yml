version: '3.8'

services:
  postgres:
    image: postgres:15.3-alpine
    restart: always
    environment:
      - POSTGRES_DB=left-it
      - POSTGRES_USER=left-it
      - POSTGRES_PASSWORD=P@ssw0rd
    ports:
      - 5432:5432
    
